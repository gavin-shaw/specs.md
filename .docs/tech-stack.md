# Tech Stack

## Overview

This is a brownfield Node.js monorepo for specs.md. It contains the npm-distributed CLI and installer in `src/`, a VS Code extension in `vs-code-extension/`, flow definitions under `src/flows/`, documentation under `docs.specs.md/`, and project memory artifacts under `memory-bank/`.

## Core Technologies

### Language

**Primary**: JavaScript and TypeScript
**Version**: Node.js 20 or newer for the CLI package; VS Code extension dependencies target Node 20-era tooling.

### Framework

**Framework**: Commander.js for CLI commands; VS Code Extension API for the extension.
**Rationale**: Commander provides mature command parsing for npm CLI distribution. The VS Code extension uses the native extension contribution model.

### Runtime

**Runtime**: Node.js
**Version**: `>=20.0.0` for `src/package.json`

## Data Layer

### Database

**Type**: File system
**Database**: Markdown, YAML, JSON, and local project files

### ORM / Data Access

**Tool**: Native filesystem helpers plus `fs-extra`, `yaml`, and `js-yaml`.
**Rationale**: specs.md stores orchestration artifacts as Git-friendly files.

### Caching

No application cache is configured.

## API Layer

### API Style

CLI commands, agent/skill markdown contracts, and VS Code extension commands.

### Documentation

Markdown and MDX documentation in `README.md`, `src/README.md`, and `docs.specs.md/`.

## Frontend

### UI Framework

**Framework**: VS Code webviews with Lit for extension UI, Ink/React for terminal UI.

### Styling

Native VS Code UI conventions for extension surfaces; terminal styling through `chalk`, `gradient-string`, and related CLI UI packages.

## Infrastructure

### Hosting

Distributed as an npm package and a VS Code extension.

### Containerization

No containerization is configured.

### CI/CD

No CI configuration was detected in the repository root during FIRE initialization.

## Development Tools

### Package Manager

**Manager**: npm

### Build Tool

**Tool**: Node.js scripts, TypeScript compiler, Vitest, markdownlint, esbuild for the VS Code webview bundle.

### Linting

**Linter**: markdownlint for flow markdown; ESLint for the VS Code extension.

### Formatting

No dedicated formatter config was detected. Preserve existing style.

## Key Dependencies

### CLI Package

- `commander`: CLI command parsing.
- `fs-extra`: installer filesystem operations.
- `yaml` and `js-yaml`: YAML parsing and writing.
- `ink` and `@inkjs/ui`: terminal UI.
- `chalk`, `figlet`, `gradient-string`, and `oh-my-logo`: terminal presentation.
- `vitest`: unit tests.
- `markdownlint-cli`: markdown validation.

### VS Code Extension

- `@types/vscode`: extension API typings.
- `lit`: webview UI.
- `esbuild`: webview bundling.
- `mocha` and `@vscode/test-electron`: extension tests.
- `eslint` and `typescript-eslint`: extension linting.

## Version Requirements

| Tool | Minimum Version | Recommended |
|------|-----------------|-------------|
| Node.js | 20.0.0 | Current active LTS |
| npm | Bundled with Node 20 | Current bundled version |
| VS Code | 1.85.0 | Latest stable |

---
Generated by specs.md FIRE Flow.
